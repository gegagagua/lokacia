'use client';

/** Owned by the billing work stream: buys VIP (7/30 days) for a listing. Rendered in the owner's listings table. */
export function VipPurchaseButton(_props: { listingId: string; vipUntil?: string | null }) {
  return null;
}
